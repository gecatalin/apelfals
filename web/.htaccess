<IfModule mod_rewrite.c>
    Options -MultiViews

    RewriteEngine On
    ##RewriteBase /proiecte/facebook-phone/web
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [QSA,L]
</IfModule>